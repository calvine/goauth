package http

import "testing"

func TestLoginGetHandler(t *testing.T) {
	type testCase struct {
		name string
	}
	testCases := []testCase{
		{
			name: "",
		},
	}
	for _, tc := range testCases {
		t.Run(tc.name, func(t *testing.T) {

		})
	}
}

func TestLoginPostHandler(t *testing.T) {
	type testCase struct {
		name string
	}
	testCases := []testCase{
		{
			name: "",
		},
	}
	for _, tc := range testCases {
		t.Run(tc.name, func(t *testing.T) {

		})
	}
}

func TestMagicLoginGet(t *testing.T) {
	type testCase struct {
		name string
	}
	testCases := []testCase{
		{
			name: "",
		},
	}
	for _, tc := range testCases {
		t.Run(tc.name, func(t *testing.T) {

		})
	}
}
