<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error Occurred</title>
</head>
<body>
    <main>
        <header>Uh oh... an error occurred</header>
        <section>
            <section>
                <p>Error code: {{ .ErrorCode }}</p>
                {{ if .ErrorMessage }}
                <p>An error occurred: {{ .ErrorMessage }}</p>
                {{ else }}
                <p>An unexpected error occurred.</p>
                {{ end }}
            </section>
            <section>
            {{ if .SupportContact }}
                <p>If this continues to occurr please contact {{ .SupportContact }} with the request id {{ .RequestID }}</p>
            {{ else }}
                <p>Oops an internal server error occurred... Request id: {{ .RequestID }}</p>
            {{ end }}
            </section>
        </section>
    </main>
</body>
</html>